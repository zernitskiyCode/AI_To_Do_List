// Конфигурация приложения
export const config = {
  // Флаг для использования mock данных (пока backend не готов)
  // Когда backend будет готов, измени на false
  USE_MOCK_DATA: true,
  
  // API endpoints (для будущего использования)
  API_ENDPOINTS: {
    WEEKLY_STATS: '/stats/weekly',
    STREAK: '/stats/streak',
  }
};
