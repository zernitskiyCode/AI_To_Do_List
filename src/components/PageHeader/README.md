# PageHeader Component

Универсальный компонент заголовка страницы, который заменяет отдельные компоненты для каждой страницы.

## Использование

```jsx
import PageHeader from '../../components/PageHeader/PageHeader';

// Для главной страницы (с уведомлениями и датой)
<PageHeader 
  title="AI Задачи"
  icon="⚡"
  variant="default"
  showDate={true}
  showNotifications={true}
  notificationCount={5}
  onNotificationClick={handleNotifications}
/>

// Для страницы статистики
<PageHeader 
  title="Статистика"
  subtitle="Ваша продуктивность"
  icon="📊"
  variant="stats"
/>

// Для страницы профиля
<PageHeader 
  title="Профиль"
  subtitle="Управление аккаунтом"
  icon="👤"
  variant="profile"
/>

// Для страницы настроек
<PageHeader 
  title="Настройки"
  subtitle="Конфигурация приложения"
  icon="⚙️"
  variant="settings"
/>

// Для страницы календаря
<PageHeader 
  title="Календарь"
  subtitle="Планирование задач"
  icon="📅"
  variant="calendar"
/>
```

## Пропсы

| Проп | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `title` | string | 'Заголовок' | Основной заголовок |
| `subtitle` | string | '' | Подзаголовок или описание |
| `icon` | string | '⚡' | Эмодзи иконка |
| `variant` | string | 'default' | Вариант стиля: 'default', 'stats', 'profile', 'settings', 'calendar' |
| `showNotifications` | boolean | false | Показать кнопку уведомлений |
| `notificationCount` | number | 0 | Количество уведомлений |
| `onNotificationClick` | function | - | Обработчик клика по уведомлениям |
| `showDate` | boolean | false | Показать текущую дату вместо subtitle |
| `date` | string | '' | Кастомная дата (если не указана, используется текущая) |
| `className` | string | '' | Дополнительные CSS классы |

## Варианты градиентов

- `default`: Фиолетовый → Синий
- `stats`: Синий → Фиолетовый  
- `profile`: Зеленый → Темно-зеленый
- `settings`: Серый → Темно-серый
- `calendar`: Оранжевый → Темно-оранжевый