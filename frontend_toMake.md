# Frontend - Календарь (Вариант 2: Balanced)

## 1. Форма создания задачи (AddTaskForm.jsx) ✅ ГОТОВО

### ✅ Реализовано:
- ✅ Компонент `DateTimePicker` с нативным `<input type="datetime-local">`
- ✅ Поле `dueDateTime` в `initialState`
- ✅ Добавлено в форму после поля "Категория"
- ✅ Опциональное поле (можно создать задачу без дедлайна)
- ✅ В `handleSubmit` отправляется `deadline` в ISO 8601 формате
- ✅ Валидация: запрет выбора даты в прошлом (`min` атрибут)

### ✅ UI элементы:
- ✅ Label: "Срок выполнения (опционально)"
- ✅ Кнопки быстрого выбора: "Сегодня", "Завтра", "Через неделю"
- ✅ Кнопка "Очистить дату" (крестик)
- ✅ Красивое отображение выбранной даты
- ✅ Кастомный placeholder вместо "дд.mm"

### 📊 Как работает:
- Пользователь выбирает дату через пресеты или календарь
- Дата конвертируется в ISO 8601: `"2026-03-27T20:59:00.000Z"`
- Отправляется в `taskData.deadline`
- Если дата не выбрана → `deadline: null`


## 2. Страница Calendar (Calendar.jsx) ✅ ГОТОВО (базовая версия)

### ✅ Что реализовано:

#### Навигация календаря:
- ✅ **← (стрелка влево)** - переход на предыдущий месяц
- ✅ **→ (стрелка вправо)** - переход на следующий месяц
- ✅ **Кнопка "Сегодня"** - быстрый возврат к текущему месяцу (между стрелками)
- ✅ **Заголовок** - отображает текущий месяц и год (например: "Март 2026")

#### Сетка календаря:
- ✅ Месячная сетка 7×6 (7 дней × 6 недель = 42 ячейки)
- ✅ Заголовки дней недели (Пн, Вт, Ср, Чт, Пт, Сб, Вс)
- ✅ Выделение сегодняшнего дня (синяя рамка)
- ✅ Дни из предыдущего/следующего месяца (серые, для заполнения сетки)

#### Отображение задач на датах:
- ✅ **Цветные точки** по приоритетам:
  - 🔴 Красная точка = high (важные задачи)
  - 🟠 Оранжевая точка = medium (средние задачи)
  - 🔵 Синяя точка = low (низкие задачи)
- ✅ **Счетчик задач** - если задач больше 3, показывает общее число
- ✅ **Группировка задач** - автоматическая группировка по датам на фронте
- ✅ **Фильтрация** - используются все задачи из `useTasks()`, фильтруются по `dueDate`

#### Созданные компоненты:
- ✅ `CalendarHeader.jsx` - навигация по месяцам
- ✅ `CalendarGrid.jsx` - сетка календаря с днями недели
- ✅ `CalendarDay.jsx` - отдельная ячейка дня с задачами
- ✅ `useCalendar.js` - хук для управления состоянием календаря
- ✅ `utils/dateHelpers.js` - утилиты для работы с датами

#### Адаптивность:
- ✅ Полностью адаптивный дизайн для мобильных устройств
- ✅ Уменьшенные размеры на маленьких экранах

### ❌ Что НЕ реализовано (оставлено на потом):

#### Интерактивность:
- ❌ **Клик на дату** - пока нет обработчика клика (только визуал)
- ❌ **DayTasksModal** - модалка со списком задач на выбранный день
- ❌ **Создание задачи на конкретный день** - из календаря нельзя создать задачу
- ❌ **Редактирование задач** - из календаря нельзя редактировать

#### Оптимизация:
- ❌ **Запросы к бэкенду** - пока все задачи загружаются сразу, нет `/tasks/by-month`
- ❌ **Автоудаление истекших задач** - задачи остаются в БД

### 🚧 Подготовка к будущему:
- ✅ В коде оставлены комментарии `// TODO:` для будущих фич
- ✅ Структура данных готова для добавления клика на дату
- ✅ Легко добавить `DayTasksModal` позже
- ✅ Можно легко интегрировать запросы к бэкенду

### 📊 Как работает сейчас:
1. Календарь получает все задачи через `useTasks()`
2. Задачи группируются по датам в `useCalendar` хуке
3. Для каждой даты показываются точки по приоритетам
4. Навигация по месяцам обновляет сетку
5. Кнопка "Сегодня" возвращает к текущему месяцу


## 3. Хуки

### useCalendar.js (новый)
- Состояние: `currentMonth`, `currentYear`, `selectedDate`
- Методы: `nextMonth()`, `prevMonth()`, `selectDate(date)`
- Генерация дней месяца с учетом предыдущего/следующего месяца
- Группировка задач по датам

### Обновить getTasks.js
- Добавить функцию `getTasksByDate(date)` → `/tasks/by-date?date=YYYY-MM-DD`
- Добавить функцию `getTasksByMonth(year, month)` → `/tasks/by-month?year=2024&month=3`


## 4. Обновления существующих компонентов

### TaskList.jsx
- Уже показывает `dueDate` - должно заработать автоматически
- Проверить форматирование даты

### AddTaskForm.jsx
- Изменить `dueDate: null` → `deadline: <выбранная дата>`
- Валидация: дата не может быть в прошлом (опционально)


## 5. Утилиты (опционально)

### utils/dateHelpers.js
- `formatDate(date)` - форматирование для отображения
- `isSameDay(date1, date2)` - сравнение дат
- `getMonthDays(year, month)` - получить все дни месяца
- `groupTasksByDate(tasks)` - группировка задач по датам


## Структура файлов

```
src/
├── components/
│   ├── Calendar/
│   │   ├── CalendarGrid.jsx
│   │   ├── CalendarDay.jsx
│   │   ├── DayTasksModal.jsx
│   │   └── Calendar.scss
│   └── Modal/
│       └── AddTaskForm.jsx (обновить)
├── hooks/
│   ├── useCalendar.js (новый)
│   └── getTasks.js (обновить)
├── pages/
│   └── Calendar/
│       └── Calendar.jsx (обновить)
└── utils/
    └── dateHelpers.js (новый, опционально)
```


## Приоритет реализации

1. **Высокий**: Форма создания с date-time picker
2. **Высокий**: Базовый календарь с сеткой месяца
3. **Средний**: Отображение задач на датах
4. **Средний**: Модалка с задачами на выбранный день
5. **Низкий**: Быстрые кнопки выбора даты
6. **Низкий**: Drag & drop (для будущего)
